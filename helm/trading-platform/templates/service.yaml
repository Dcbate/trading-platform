apiVersion: v1
kind: Service
metadata:
  name: {{ include "trading-platform.fullname" . }}
spec:
  selector:
    app: {{ include "trading-platform.fullname" . }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
