# EXAMPLE ONLY — placeholder values, safe to commit. Real values are injected at deploy time
# (by CI from GCP Secret Manager, or via `kubectl create secret` / `helm upgrade --set-string`)
# and must never be committed. See docs/DEPLOYMENT.md for the GCP Secret Manager pattern.
apiVersion: v1
kind: Secret
metadata:
  name: trading-platform-secrets
  namespace: trading-platform
type: Opaque
stringData:
  SPRING_DATASOURCE_USERNAME: "placeholder-set-me"
  SPRING_DATASOURCE_PASSWORD: "placeholder-set-me"
  JWT_SECRET: "placeholder-set-me"
  GEMINI_API_KEY: "placeholder-set-me"
  CLAUDE_API_KEY: "placeholder-set-me"
  SENDGRID_API_KEY: "placeholder-set-me"
  SLACK_WEBHOOK_URL: "placeholder-set-me"
