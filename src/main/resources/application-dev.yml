# Local development convenience profile: disables JWT enforcement so the API
# can be exercised with plain curl/Postman without minting a token first.
# NEVER active in production — SecurityConfig only wires the permit-all chain
# when this profile is present; the default chain always requires a valid JWT.
security:
  dev-mode: true

logging:
  level:
    com.dcbate.tradingplatform: DEBUG
